# Treasure-Diver-3D
A 3D Mini game made with OpenGL in python

To run the game install python in your system and external dependencies mentioned below,
PyOpenGL: pip install PyOpenGL PyOpenGL_accelerate
FreeGLUT binaries: pip install freeglut
